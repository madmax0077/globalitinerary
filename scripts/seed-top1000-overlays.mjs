/**
 * DISABLED — do not invent landmark names.
 * Template sights harmed trust (e.g. "Grindelwald cathedral").
 * Use Wikivoyage-sourced sights or hand packs (real-city-packs / top300-sights-data) only.
 *
 * Previously: node scripts/seed-top1000-overlays.mjs
 */
console.error(
  "DISABLED: seed-top1000-overlays invents template landmarks. Use real Wikivoyage/packs instead.",
);
process.exit(1);
